experiment_name: mlp_vae_toy

model:
  model_type: mlp_vae
  latent_dim: 2
  hidden_dim: 32
  depth: 1

training:
  epochs: 80
  lr: 1.0e-3
  scheduler: cosine
  warmup_epochs: 10
  kl_weight: 0.01
  kl_warmup_epochs: 20

ood:
  primary_score: elbo
  knn_k: 5

wandb:
  tags: [ood, mlp_vae, toy]
  save_code: false
